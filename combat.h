#pragma once
#include "actors.h"
#include "linked_list.h"
#include <memory>
#include <string>
#include <vector>
#include <algorithm>
#include <list>
using namespace std;

void begin_combat(linkedList &list, vector<unique_ptr<Hero>> &heroes, vector<unique_ptr<Monster>> &monsters) { 
			cout << "Hero 1 HP: " << heroes.at(0)->getName() << endl;			


	}

